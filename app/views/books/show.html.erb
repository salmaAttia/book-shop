<h2 align="center">Title: <%= @book.name %></h2>
<div class="well col-xs-8 col-xs-offset-2">
  <h4 class="center description"><strong>Description:</strong></h4>
  <hr>
  <%= simple_format(@book.description) %>
  <div class="article-actions">
    <%if can? :edit, Book and can? :delete, Book%>
      <%= link_to "Edit this book", edit_book_path(@book), class: "btn btn-xs btn-primary" %>
      <%= link_to "Delete this book", book_path(@book), method: :delete, 
                                        data: { confirm: "Are you sure you want to delete the book?"}, 
                                        class: "btn btn-xs btn-danger" %>
    <%end%>                                  
    <%= link_to "View all books", books_path, class: "btn btn-xs btn-success" %>
  </div>
</div>